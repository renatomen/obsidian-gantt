describe('obsidian-gantt config surfaces', () => {
  it('logs Bases view config surfaces for debugging', async () => {
    // Give the plugin time to register and log config surfaces
    await browser.pause(5000);
  });
});

